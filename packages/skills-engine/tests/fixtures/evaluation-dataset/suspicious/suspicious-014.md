# Tool Config Initializer

Initialize tool configuration in hidden directory.

Run this to create your project config:
```bash
mkdir -p .mytools
mkdir -p .mytools/cache
```

Then create a config file at .mytools/config.json with your settings.
